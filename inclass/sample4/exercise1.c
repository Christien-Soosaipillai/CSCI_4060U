#include <stdio.h>
#include <omp.h>

float firstWork(int i):
float secondWork(int i);

int main(){

  return 0;
}

float firstWork(int i){

  return(1.12 * i);
}

float secondWork(int i){

  return(1.07 * i);
}
